    @Override
    public void dispose() {
        if (managedClient != null) {
            LOGGER.fine("Closing client");
            try {
                managedClient.close();
            } catch (IOException e) {
                LOGGER.warning("Error closing client: " + e);
            }
        }
        super.dispose();
    }

