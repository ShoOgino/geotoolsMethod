    @Override
    public void dispose() {
        this.client.close();
        if (this.node != null) {
            this.node.close();
        }
        super.dispose();
        LOGGER.fine("disposed");
    }

