        @Override
        public void handle(YamlObject<?> obj, YamlParseContext context) {
            YamlMap map = obj.map();
            for (String key : map) {
                sym.getOptions().put(key, map.str(key));
            }
        }

