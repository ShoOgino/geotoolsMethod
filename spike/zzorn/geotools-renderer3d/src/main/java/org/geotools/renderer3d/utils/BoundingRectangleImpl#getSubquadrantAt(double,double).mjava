    public int getSubquadrantAt( double x, double y )
    {
        if ( isInside( x, y ) )
        {
            return getSubsectorAt( x, y );
        }
        else
        {
            return -1;
        }
    }

