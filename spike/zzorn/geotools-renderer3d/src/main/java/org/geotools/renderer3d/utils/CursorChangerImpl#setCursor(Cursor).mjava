    private void setCursor( Cursor cursor )
    {
        if ( cursor == null )
        {
            cursor = EMPTY_CURSOR;
        }

        if ( myComponent != null )
        {
            myComponent.setCursor( cursor );
        }
    }

