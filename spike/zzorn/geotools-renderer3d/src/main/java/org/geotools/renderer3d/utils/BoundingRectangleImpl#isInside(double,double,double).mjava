    public boolean isInside( final double x, final double y, final double radius )
    {
        return x >= myX1 + radius &&
               x < myX2 - radius &&
               y >= myY1 + radius &&
               y < myY2 - radius;
    }

