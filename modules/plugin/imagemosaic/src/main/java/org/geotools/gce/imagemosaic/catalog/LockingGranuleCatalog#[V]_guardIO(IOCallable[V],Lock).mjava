    private <V> V guardIO(IOCallable<V> operation, Lock lock) throws IOException {
        try {
            lock.lock();
            return operation.call();
        } finally {
            lock.unlock();
        }
    }

