    private void guardIO(IORunnable operation, Lock lock) throws IOException {
        try {
            lock.lock();
            operation.run();
        } finally {
            lock.unlock();
        }
    }

