    @Override
    public void dispose() {
        Lock lock = rwLock.writeLock();
        try {
            lock.lock();
            delegate.dispose();
        } finally {
            lock.unlock();
        }
    }

