		@Override
		protected void handleEnd(Collection results) throws IOException {
			try{
				transaction.commit();
			}
			finally{
				transaction.close();
			}		
			indexingPostamble();
			super.handleEnd(results);
		}

