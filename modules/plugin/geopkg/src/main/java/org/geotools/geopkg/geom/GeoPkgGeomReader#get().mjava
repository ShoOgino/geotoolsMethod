    public Geometry get() throws IOException {
        if (header == null) {
            header = readHeader();
        }
        if (geometry == null) {
            geometry = read();
        }
        return geometry;
    }

