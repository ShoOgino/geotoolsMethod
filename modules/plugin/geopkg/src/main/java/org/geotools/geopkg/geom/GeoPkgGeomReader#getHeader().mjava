    public GeometryHeader getHeader() throws IOException {
        if (header == null) {
            try {
                header = readHeader();
            } catch (ParseException e) {
                throw new IOException(e);
            }
        }
        return header;
    }

