    private int normalizeTile(int tile, int min, int max) {
        if (tile < min) {
            return min;
        } else if (tile > max) {
            return max;
        } else {
            return tile;
        }
    }

