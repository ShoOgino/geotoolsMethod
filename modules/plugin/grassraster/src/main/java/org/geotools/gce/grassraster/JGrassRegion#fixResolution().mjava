    /** calculates the resolution from the boundaries of the region and the rows and cols. */
    private void fixResolution() {
        we_res = (east - west) / cols;
        ns_res = (north - south) / rows;
    }

