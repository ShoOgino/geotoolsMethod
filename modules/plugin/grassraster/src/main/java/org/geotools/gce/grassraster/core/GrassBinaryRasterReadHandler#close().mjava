    /** closes the I/O streams. */
    @Override
    public void close() throws IOException {
        if (imageIS != null) {
            imageIS.close();
            imageNullFileIS.close();
        }
    }

