    @Override
    public void remove() throws IOException {
        try {
            super.remove();
        } catch (IOException e) {
            state.getTransaction().rollback();
            throw e;
        }
    }

