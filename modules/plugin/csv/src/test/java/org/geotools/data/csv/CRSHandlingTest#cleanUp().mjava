    @AfterClass
    public static void cleanUp() {
        if (tmp.exists()) {
            File[] allContents = tmp.listFiles();
            if (allContents != null) {
                for (File file : allContents) {
                    file.delete();
                }
            }
            tmp.delete();
        }
    }

