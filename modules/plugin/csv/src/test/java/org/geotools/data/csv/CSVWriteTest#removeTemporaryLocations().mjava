    @After
    public void removeTemporaryLocations() throws IOException {
        File list[] = tmp.listFiles();
        if (list != null) {
            for (File file : list) {
                file.delete();
            }
        }
        tmp.delete();
    }

