    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        for (int j : index) {
            result = prime * result + j;
        }
        result = prime * result + ((variableName == null) ? 0 : variableName.hashCode());
        return result;
    }

