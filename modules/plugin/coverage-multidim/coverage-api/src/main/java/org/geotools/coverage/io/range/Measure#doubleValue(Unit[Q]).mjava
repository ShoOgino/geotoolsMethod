    /**
     * Double representation of {@link #getValue()} in the requested unit (values outside of the range {@link Double#MIN_VALUE} -
     * {@link Double#MAX_VALUE} are represented as infinite).
     * 
     * @param unit to convert to double
     */
    public double doubleValue(Unit<Q> unit) {
        Number number = numericValue(unit);
        return number.doubleValue();
    }

