    @Override
    public void FeatureSetFieldDateTime(
            Object feature,
            int field,
            int year,
            int month,
            int day,
            int hour,
            int minute,
            int second,
            int tz) {
        OGR_F_SetFieldDateTime(
                (Pointer<?>) feature, field, year, month, day, hour, minute, second, tz);
    }

