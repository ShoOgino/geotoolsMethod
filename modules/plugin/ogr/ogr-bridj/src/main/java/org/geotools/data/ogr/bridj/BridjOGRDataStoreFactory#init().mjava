    public static void init() {
        try {
            if (!INIT) {
                GdalInit.init();

                // perform OGR format registration once
                if (OGRGetDriverCount() == 0) {
                    OGRRegisterAll();
                }
            }
        } finally {
            INIT = true;
        }
    }

