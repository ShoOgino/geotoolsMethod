    private void dieDieDIE(File file) {
        if (file.exists()) {
            if (file.delete()) {
                // dead
            } else {
                // System.out.println("Couldn't delete " + file);
                file.deleteOnExit(); // dead later
            }
        }
    }

