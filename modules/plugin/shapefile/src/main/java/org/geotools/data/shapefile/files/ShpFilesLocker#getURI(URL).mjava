    URI getURI(URL url) {
        try {
            return url.toURI();
        } catch (URISyntaxException e) {
            // may fail if URL does not conform to RFC 2396
        }
        return null;
    }

