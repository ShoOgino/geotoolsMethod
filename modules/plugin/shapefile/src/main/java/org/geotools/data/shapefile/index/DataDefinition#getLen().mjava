    /** Gets the max len of the data */
    public int getLen() {
        int len = 0;

        Field field = null;

        for (Field value : this.fields) {
            field = value;
            len += field.getLen();
        }

        return len;
    }

