    /** Gets the max len of the data */
    public int getLen() {
        int len = 0;

        Field field = null;

        for (int i = 0; i < this.fields.size(); i++) {
            field = this.fields.get(i);
            len += field.getLen();
        }

        return len;
    }

