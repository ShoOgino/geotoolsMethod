    private int getNextOffset() throws IOException {
        if (currentShape >= 0) {
            return shxReader.getOffsetInBytes(currentShape);
        } else {
            return record.end;
        }
    }

