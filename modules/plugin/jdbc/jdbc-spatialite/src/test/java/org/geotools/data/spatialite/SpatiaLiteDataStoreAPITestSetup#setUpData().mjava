    @Override
    protected void setUpData() throws Exception {
        //kill all the data
        super.setUpData();
        try {
            dropRecreatedTable();
        } catch (SQLException e) {
        }
        try {
            dropDataTypesTable();
        } catch (SQLException e) {
        }
    }

