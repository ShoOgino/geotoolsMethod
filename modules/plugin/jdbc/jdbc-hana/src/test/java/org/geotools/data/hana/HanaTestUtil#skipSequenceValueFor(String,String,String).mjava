    public void skipSequenceValueFor(String schemaName, String tableName, String columnName)
            throws SQLException {
        execute(
                "SELECT "
                        + HanaTestUtil.nextSequenceValSql(schemaName, tableName, columnName)
                        + " FROM DUMMY");
    }

