    public void createRegisteredTable(String schemaName, String tableName, String[]... cols)
            throws SQLException {
        createTable(schemaName, tableName, cols);
        createSequencesForTable(schemaName, tableName);
    }

