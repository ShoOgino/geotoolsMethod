    private Geometry create(SDO_GEOMETRY oraGeom, GeometryFactory gf) {
        return SDO.create(
                gf,
                oraGeom.gType,
                oraGeom.srid,
                oraGeom.ptType,
                oraGeom.elemInfo,
                oraGeom.ordinates);
    }

