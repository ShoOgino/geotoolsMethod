    protected final void setUpData() throws Exception {
        // remove the srs
        try (Connection con = getConnection()) {
            try {

                removeSRS(con);
            } catch (SQLException e) {
            }
            // create the SRS
            createSRS(con);
        }
        super.setUpData();
    }

