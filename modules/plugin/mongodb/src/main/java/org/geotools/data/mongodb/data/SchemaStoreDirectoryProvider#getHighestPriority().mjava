    public static SchemaStoreDirectory getHighestPriority() {
        return directoriies
                .stream()
                .sorted((o1, o2) -> o2.getPriority() - o1.getPriority())
                .findFirst()
                .get();
    }

