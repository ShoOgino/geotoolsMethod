    List<?> toList(Coordinate[] cs) {
        BasicDBList l = new BasicDBList();
        for (Coordinate c : cs) {
            BasicDBList m = new BasicDBList();
            m.add(c.x);
            m.add(c.y);
            l.add(m);
        }
        return l;
    }

