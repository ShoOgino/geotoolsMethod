    private static void resursiveDelete(File f) {
        File[] l = f.listFiles();
        if (l != null) {
            for (File c : l) {
                resursiveDelete(c);
            }
        }
        f.delete();
    }

