    public void reset() {
        baseFeatureSource = null;
        featureSourceCache = new HashMap<>();
        indexMapping = new HashMap<>();
        supportedHints = null;
        queryCapabilities = null;
        featureTyp = null;
    }

