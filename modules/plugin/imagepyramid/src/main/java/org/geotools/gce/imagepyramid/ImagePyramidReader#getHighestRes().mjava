    /**
     * Returns the highest resolution available.
     * 
     * TODO The instance variable comes from {@link AbstractGridCoverage2DReader}, so maybe we
     * should move the getter there.
     * */
    double[] getHighestRes() {
        return highestRes;
    }

