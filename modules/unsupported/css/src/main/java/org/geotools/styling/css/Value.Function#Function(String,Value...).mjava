        /**
         * Builds a function
         *
         * @param name
         * @param parameters
         */
        public Function(String name, Value... parameters) {
            this(name, Arrays.asList(parameters));
        }

