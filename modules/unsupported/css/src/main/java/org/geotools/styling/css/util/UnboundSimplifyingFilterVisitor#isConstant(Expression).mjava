    @Override
    protected boolean isConstant(Expression ex) {
        if (ex instanceof EnvFunction) {
            return false;
        } else {
            return super.isConstant(ex);
        }
    }

