    public String getDirectiveValue(String name) {
        for (Directive d : directives) {
            if (name.equals(d.getName())) {
                return d.getValue();
            }
        }

        return null;
    }

