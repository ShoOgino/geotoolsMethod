    @Override
    public List<RasterManager> create() {
        // do one last internal create
        if (!slices.isEmpty()) this.descriptors.add(new RasterDescriptor(slices));
        return null;
    }

