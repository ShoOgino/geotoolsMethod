    /// This variable is used by `gamma' and `lgamma'.
    public int signgam() {
        try {
            return (int)
                    BridJ.getNativeLibrary("osr").getSymbolPointer("signgam").as(int.class).get();
        } catch (Throwable $ex$) {
            throw new RuntimeException($ex$);
        }
    }

