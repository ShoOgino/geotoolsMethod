    public void close() {
        // already closed?
        if (source == null) {
            return;
        }

        // return to the pool or destroy
        if (pool != null) {
            pool.returnObject(this);
        } else {
            destroy();
        }
    }

