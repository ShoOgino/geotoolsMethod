    void destroy() {
        if (source != null) {
            ogr.DataSourceRelease(source);
        }

        // release references
        pool = null;
        source = null;
        ogr = null;
        tracer = null;
    }

