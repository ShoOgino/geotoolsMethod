	/**
	 * Sets the JDBC database login credentials.
	 * 
	 * @param username
	 *            the username
	 * @param password
	 *            the password
	 */
	public void setPassword(String password) {

		_password = password;
	}

