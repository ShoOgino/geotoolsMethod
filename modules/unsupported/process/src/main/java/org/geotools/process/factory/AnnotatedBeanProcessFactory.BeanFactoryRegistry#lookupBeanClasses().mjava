        public Class<? extends T>[] lookupBeanClasses() {
            return getFactories(getBeanClass(), null, null)
                    .map(Object::getClass)
                    .toArray(Class[]::new);
        }

