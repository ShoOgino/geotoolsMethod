    /**
     * {@inheritDoc}
     *
     * <p>This implementation calls {@code run(List<T> args)} method with the list of accumulated
     * arguments.
     */
    public final void run() {
        run(flush());
    }

