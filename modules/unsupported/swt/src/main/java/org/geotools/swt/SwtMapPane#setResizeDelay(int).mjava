    /**
     * Set the length of the delay period between the pane being resized and the next repaint.
     *
     * <p>The map pane imposes a delay between resize events and repainting to avoid flickering of
     * the display during drag-resizing.
     *
     * @param delay the delay in milliseconds; if {@code <} 0 the default delay period will be set
     */
    public void setResizeDelay(int delay) {
        if (delay < 0) {
            resizingPaintDelay = DEFAULT_RESIZING_PAINT_DELAY;
        } else {
            resizingPaintDelay = delay;
        }
    }

