    /**
     * Move the image currently displayed by the map pane from its current origin (x,y) to (x+dx,
     * y+dy). This method allows dragging the map without the overhead of redrawing the features
     * during the drag. For example, it is used by {@link org.geotools.swing.tool.PanTool}.
     *
     * @param dx the x offset in pixels
     * @param dy the y offset in pixels.
     */
    public void moveImage(int dx, int dy) {
        imageOrigin.translate(dx, dy);
        redrawBaseImage = false;
        if (!isDisposed()) redraw();
    }

