    /**
     * Returns the colour which is used to draw the bounding box of the currently active cursor
     * tool. The bounding box is drawn in xor mode.
     *
     * @return the colour used for the tool's bounding box
     */
    public Color getCursorToolColor() {
        return cursorToolColor;
    }

