    /**
     * Display a dummy exception. This method is provided only as an easy way to test the dialog
     * appearance from the command line.
     *
     * @param args ignored
     */
    public static void main(final String[] args) {
        show(new Shell(Display.getDefault()), new Exception());
    }

