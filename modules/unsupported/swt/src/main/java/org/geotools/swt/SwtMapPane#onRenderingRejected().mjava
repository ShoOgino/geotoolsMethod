    /**
     * Called when a rendering request has been rejected. This will be common, such as when the user
     * pauses during drag-resizing fo the map pane. The base implementation does nothing. It is
     * provided for sub-classes to override if required.
     */
    public void onRenderingRejected() {
        // do nothing
    }

