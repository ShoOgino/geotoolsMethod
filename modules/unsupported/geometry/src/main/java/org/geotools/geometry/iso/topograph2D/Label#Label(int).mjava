    /**
     * Construct a Label with a single location for both Geometries. Initialize the locations to
     * Null
     */
    public Label(int onLoc) {
        elt[0] = new TopologyLocation(onLoc);
        elt[1] = new TopologyLocation(onLoc);
    }

