    /* (non-Javadoc)
     * @see org.opengis.geometry.complex.Complex#getSubComplexes()
     */
    public Complex[] getSubComplexes() {
        return (this.subComplex == null)
                ? null
                : this.subComplex.toArray(new Complex[this.subComplex.size()]);
    }

