    public static Point2D evaluate(Point2D p0, Point2D p1, Point2D eval) {
        return new Point2D.Double(
                (1.0 - eval.getX()) * p0.getX() + eval.getX() * p1.getX(),
                (1.0 - eval.getY()) * p0.getY() + eval.getY() * p1.getY());
    }

