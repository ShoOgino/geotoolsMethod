    /**
     * Creates a <code>UnsupportedDimensionException</code> with <code>e</code>s detail message.
     *
     * @param e an exception that occurred while trying to operate a function which is not operable
     *     for that coordiante dimension
     */
    public UnsupportedDimensionException(Exception e) {
        this(e.toString());
    }

