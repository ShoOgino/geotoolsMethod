    /**
     * @param min0
     * @param max0
     * @param d
     * @return
     */
    private static boolean intersects(double min0, double max0, double d) {
        return (min0 <= d) && (max0 >= d);
    }

