    /* (non-Javadoc)
     * @see org.opengis.geometry.complex.Complex#getSuperComplexes()
     */
    public Complex[] getSuperComplexes() {
        return (this.superComplex == null)
                ? null
                : this.superComplex.toArray(new Complex[this.superComplex.size()]);
    }

