    /**
     * GenericCurve: {parameterization(startParam()) = startPoint()}; {parameterization(endParam())
     * = endPoint()}; {length() = endParam() - startParam()} returns the length => {length() =
     * this.endParam() - this.startParam()} Returns the complete length of the CurveSegment
     *
     * @return double
     * @version Implementation OK
     */
    public double length() {
        return DoubleOperation.subtract(this.getEndParam(), this.getStartParam());
    }

