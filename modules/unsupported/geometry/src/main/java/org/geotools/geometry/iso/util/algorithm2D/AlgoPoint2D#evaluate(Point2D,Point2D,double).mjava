    public static Point2D evaluate(Point2D p0, Point2D p1, double r) {
        return new Point2D.Double(
                (1.0 - r) * p0.getX() + r * p1.getX(), (1.0 - r) * p0.getY() + r * p1.getY());
    }

