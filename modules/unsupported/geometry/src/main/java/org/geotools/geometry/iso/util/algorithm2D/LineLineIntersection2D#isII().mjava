    public boolean isII() {
        /**
         * the interiors of the segments intersect if they intersect at one node or if their
         * interior overlap number and two itscs are Double.NaN
         */
        return intersectInteriorInteriorPoint() || overlapsInterior();
    }

