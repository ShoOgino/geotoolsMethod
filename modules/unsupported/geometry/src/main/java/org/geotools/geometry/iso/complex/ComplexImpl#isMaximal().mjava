    /**
     * The Boolean valued operation "isMaximal" shall return TRUE if and only if this Complex is
     * maximal. Complex::isMaximal() : Boolean
     *
     * @return true if this complex is maximal
     */
    public boolean isMaximal() {
        return ((superComplex == null) || superComplex.isEmpty());
    }

