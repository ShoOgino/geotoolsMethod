    /**
     * Computes all self-intersections between edges in a set of edges, allowing client to choose
     * whether self-intersections are computed.
     *
     * @param edges a list of edges to test for intersections
     * @param si the SegmentIntersector to use
     * @param testAllSegments true if self-intersections are to be tested as well
     */
    public abstract void computeIntersections(
            List edges, SegmentIntersector si, boolean testAllSegments);

