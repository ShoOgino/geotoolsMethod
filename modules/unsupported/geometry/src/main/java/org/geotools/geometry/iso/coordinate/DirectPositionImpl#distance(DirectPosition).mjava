    /**
     * Calculates the distance to another direct position
     *
     * @param p direct Position
     * @return Distance
     */
    public double distance(DirectPosition p) {
        return AlgoPointND.getDistance(this.coordinate, p.getCoordinate());
    }

