    /**
     * Returns <code>true</code> if <code>other</code> has the same values for x, y and z.
     *
     * @param other a <code>Coordinate</code> with which to do the 3D comparison.
     * @return <code>true</code> if <code>other</code> is a <code>Coordinate</code> with the same
     *     values for x, y and z.
     */
    public boolean equals3D(Coordinate other) {
        return (x == other.x)
                && (y == other.y)
                && ((z == other.z) || (Double.isNaN(z) && Double.isNaN(other.z)));
    }

