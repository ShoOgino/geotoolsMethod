    /**
     * Returns a hash code for a double value, using the algorithm from Joshua Bloch's book
     * <i>Effective Java"</i>
     */
    public static int hashCode(double x) {
        long f = Double.doubleToLongBits(x);
        return (int) (f ^ (f >>> 32));
    }

