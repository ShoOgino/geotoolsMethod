    /**
     * Returns true, if the Position is representated as a PointReference. Returns false, if the
     * Position is representated as a DirectPoint.
     *
     * @return true if the Position is representated as a PointReference
     */
    public boolean hasPoint() {
        return (this.position instanceof PointImpl);
    }

