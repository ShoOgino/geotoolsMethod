    protected Simplex2D(Node2D[] p, int n) {
        this.initialize(n);
        // assert p.length >= n;
        for (int i = 0; i < n; ++i) {
            point[i] = new Node2D(p[i]);
        }
    }

