    /**
     * Link the DirectedEdges at the nodes of the graph. This allows clients to link only a subset
     * of nodes in the graph, for efficiency (because they know that only a subset is of interest).
     */
    public void linkAllDirectedEdges() {
        for (Iterator nodeit = nodes.iterator(); nodeit.hasNext(); ) {
            Node node = (Node) nodeit.next();
            ((DirectedEdgeStar) node.getEdges()).linkAllDirectedEdges();
        }
    }

