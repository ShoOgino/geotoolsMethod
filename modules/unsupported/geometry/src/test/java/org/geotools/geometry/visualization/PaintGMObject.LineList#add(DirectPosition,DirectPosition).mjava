        public void add(DirectPosition p1, DirectPosition p2) {
            Line line =
                    new Line(
                            p1.getOrdinate(0),
                            p1.getOrdinate(1),
                            p2.getOrdinate(0),
                            p2.getOrdinate(1));
            this.list.add(line);
        }

