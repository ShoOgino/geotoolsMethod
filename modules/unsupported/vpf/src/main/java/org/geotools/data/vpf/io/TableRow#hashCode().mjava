    public int hashCode() {
        int code = 0;

        if ((fieldsArr == null) || (fieldsArr.length == 0)) {
            code = super.hashCode();
        } else {
            for (RowField rowField : fieldsArr) {
                code += rowField.hashCode();
            }
        }

        return code;
    }

