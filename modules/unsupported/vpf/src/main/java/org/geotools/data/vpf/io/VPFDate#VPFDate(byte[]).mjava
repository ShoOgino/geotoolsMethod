    /**
     * Creates a new <code>VPFDate</code> instance.
     *
     * @param date a <code>byte[]</code> value
     */
    public VPFDate(byte[] date) {
        dateBin = date.clone();
        initialize();
    }

