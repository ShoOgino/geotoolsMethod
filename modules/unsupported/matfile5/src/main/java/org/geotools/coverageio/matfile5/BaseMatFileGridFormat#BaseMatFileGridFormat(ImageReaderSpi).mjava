    /**
     * Constructor for the {@code BaseMatFileGridFormat}. It is invoked by the
     * underlying implementations.
     * 
     * @param spi
     *                the format specific {@code ImageReaderSpi} instance
     */
    protected BaseMatFileGridFormat(ImageReaderSpi spi) {
        this.spi = spi;
        setInfo();
    }

