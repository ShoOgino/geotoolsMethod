    private boolean isValid(String geohash) {
        return geohash != null
                && geohash.equals(
                        GeoHash.encodeHash(GeoHash.decodeHash(geohash), geohash.length()));
    }

