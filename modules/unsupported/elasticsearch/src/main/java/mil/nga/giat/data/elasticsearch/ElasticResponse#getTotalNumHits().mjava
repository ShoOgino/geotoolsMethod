    public long getTotalNumHits() {
        final long total;
        if (results != null && results.getTotal() != null) {
            total = results.getTotal();
        } else {
            total = 0l;
        }
        return total;
    }

