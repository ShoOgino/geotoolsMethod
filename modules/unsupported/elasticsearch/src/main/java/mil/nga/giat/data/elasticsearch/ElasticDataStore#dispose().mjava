    @Override
    public void dispose() {
        LOGGER.fine("disposing");
        this.client.close();
        if (this.node != null) {
            this.node.close();
        }
        super.dispose();
    }

