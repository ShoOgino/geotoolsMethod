    @Override
    public void dispose() {
        this.client.close();
        if (this.node != null) {
            this.node.stop();
            this.node.close();
        }
        super.dispose();
        LOGGER.fine("disposed");
    }

