    @AfterClass
    public static void suiteTearDown() {
        if (dataStore != null) {
            dataStore.dispose();
        }
        //TODO: Need to close only after all tests in all suites have run
        //        node.close();
    }

