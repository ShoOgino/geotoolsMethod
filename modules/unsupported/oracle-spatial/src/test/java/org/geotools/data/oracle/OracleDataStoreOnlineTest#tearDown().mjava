    /*
     * @see TestCase#tearDown()
     */
    protected void tearDown() throws Exception {    	
    	//reset();
        if(conn != null) 
            conn.close();
        if(dstore != null)
            dstore.dispose();
    }

