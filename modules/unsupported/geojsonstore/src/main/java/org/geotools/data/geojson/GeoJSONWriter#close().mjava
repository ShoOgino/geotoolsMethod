    @Override
    public void close() throws IOException {
        try {
            if (inArray) {
                generator.writeEndArray();
                generator.writeEndObject();
            }

            generator.close();
        } finally {
            out.close();
        }
    }

