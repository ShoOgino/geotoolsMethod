    public Name getTypeName() {
        if (namespaceURI != null) {
            return new NameImpl(namespaceURI, typeName.getLocalPart());
        } else {
            return typeName;
        }
    }

