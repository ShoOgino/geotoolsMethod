  @Override
  public int hashCode() {
    return new HashCodeBuilder().append(serviceName).append(version).append(parameters).append(numberOfMonitors)
        .append(monitors).append(additionalProperties).toHashCode();
  }

