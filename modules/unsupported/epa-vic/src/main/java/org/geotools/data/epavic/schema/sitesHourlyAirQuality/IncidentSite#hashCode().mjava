  @Override
  public int hashCode() {
    return new HashCodeBuilder().append(emergencyUrl).append(incidentIcon).append(latitude).append(longitude)
        .append(measurements).append(name).append(region).append(siteId).append(siteType).append(additionalProperties)
        .toHashCode();
  }

