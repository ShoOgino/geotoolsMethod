  @Override
  public boolean equals(Object other) {
    if (other == this) {
      return true;
    }
    if ((other instanceof Monitors) == false) {
      return false;
    }
    Monitors rhs = ((Monitors) other);
    return new EqualsBuilder().append(serviceName, rhs.serviceName).append(version, rhs.version)
        .append(parameters, rhs.parameters).append(numberOfMonitors, rhs.numberOfMonitors)
        .append(monitors, rhs.monitors).append(additionalProperties, rhs.additionalProperties).isEquals();
  }

