    /**
     * Closes down this reader
     */
    void close() {
        if (is != null) {
            try {
                is.close();
            } catch(IOException e) {
                // ignore
            }
            file.delete();
        }
    }

