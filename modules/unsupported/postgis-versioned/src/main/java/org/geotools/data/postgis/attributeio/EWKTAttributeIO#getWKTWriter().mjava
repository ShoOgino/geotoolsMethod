    /**
     * Lazily initialize the WKTWriter
     *
     */
    private WKTWriter getWKTWriter() {
        if (writer == null) {
            writer = new WKTWriter();
        }

        return writer;
    }

