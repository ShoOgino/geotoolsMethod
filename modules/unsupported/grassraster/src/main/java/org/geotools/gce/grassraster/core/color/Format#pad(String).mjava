    /**
     *
     * @param r
     *
     * @return
     */
    private String pad( String r ) {
        String p = repeat(' ', width - r.length());

        if (leftAlign) {
            return pre + r + p + post;
        } else {
            return pre + p + r + post;
        }
    }

