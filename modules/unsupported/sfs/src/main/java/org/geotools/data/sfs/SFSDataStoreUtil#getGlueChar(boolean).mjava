    /**
     * Method used to construct the URL from query
     *
     * @param fcFlag
     * @return String
     */
    private static String getGlueChar(boolean fcFlag) {
        if (fcFlag) {
            return "";
        }
        return "&";
    }

