    /**
     * It processes the incoming URL and gets the response in the form of stream. It then converts
     * the stream into a string(it should be in json format)
     *
     * @param
     */
    public final void processCapabilities() throws IOException {
        processCapabilities(resourceToString("capabilities", null));
    }

