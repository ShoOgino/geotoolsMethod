    /**
     * Check if this {@link EFeatureIDFactory factory} contains IDs for given {@link
     * Resource#getURI() URI}.
     *
     * @param eURI - the {@link Resource#getURI() URI}.
     * @return <code>true</code> if IDs exists.
     */
    public boolean contains(URI eURI);

