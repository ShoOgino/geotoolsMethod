    @Override
    protected void doInvalidate(boolean deep) { 
        if (deep) {
            for (EStructureInfo<?> it : eDomainMap.values()) {
                it.doInvalidate(true);
            }
        }
    }

