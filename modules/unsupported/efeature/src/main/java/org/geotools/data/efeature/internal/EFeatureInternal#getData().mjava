    public Feature getData() {
        verify();
        //
        // Initialize data?
        //
        if (data == null) {
            data = new SimpleFeatureDelegate();
        }
        return data;
    }

