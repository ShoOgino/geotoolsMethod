    /**
     * Check if this factory create IDs for given {@link EFeature} instance
     *
     * @param eObject - a {@link EFeature} instance or a {@link EObject} containing {@link EFeature}
     *     compatible data.
     * @return <code>true</code> if this factory create IDs for given {@link EFeature} instance.
     */
    public boolean creates(EObject eObject);

