    @Override
    public EFeatureReader getFeatureReader(Query query, Transaction transaction)
            throws IOException {
        return (EFeatureReader) super.getFeatureReader(query, transaction);
    }

