    /** Dispose this {@link EStructureInfo structure} and all it's children. */
    protected void dispose() {
        isDisposed = true;
        doDispose();
        eFactory = null;
        eContext = null;
        eListeners.clear();
        eListeners = null;
    }

