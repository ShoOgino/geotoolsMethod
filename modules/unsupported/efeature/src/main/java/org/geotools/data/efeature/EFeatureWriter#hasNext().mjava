    @Override
    public boolean hasNext() throws IOException {
        if (eReader == null) {
            return false; // writer has been closed
        }
        if (isModified()) {
            write();
        }
        return eReader.hasNext();
    }

