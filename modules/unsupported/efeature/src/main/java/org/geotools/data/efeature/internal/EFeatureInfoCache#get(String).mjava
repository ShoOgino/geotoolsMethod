    public final EFeatureInfo get(String eKey) {
        WeakReference<EFeatureInfo> eInfo = eCache.get(eKey);
        if( eInfo != null) {
            return eInfo.get();
        }
        return null;
    }

