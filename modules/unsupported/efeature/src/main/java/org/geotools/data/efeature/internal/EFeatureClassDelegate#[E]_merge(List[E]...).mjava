    private <E> EList<E> merge(List<E>... lists) {
        EContentsEList<E> eList = 
            new EContentsEList<E>(this);
        for(List<E> it : lists) {
            eList.addAll(it);
        }
        return eList;
    }

