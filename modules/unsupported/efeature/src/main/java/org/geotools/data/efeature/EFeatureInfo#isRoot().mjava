    /**
     * Check if {@link EFeature} is a root.
     *
     * <p>A {@link EFeature} roots are not referenced (contained).
     *
     * @return <code>true</code> if EObject root.
     */
    public boolean isRoot() {
        return eIsRoot;
    }

