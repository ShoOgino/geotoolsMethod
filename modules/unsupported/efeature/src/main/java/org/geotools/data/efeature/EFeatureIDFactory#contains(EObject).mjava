    /**
     * Check if this {@link EFeatureIDFactory factory} contains an ID for given {@link EObject
     * object}.
     *
     * @param eObject - the {@link EObject} instance.
     * @return <code>true</code> if an ID exists.
     */
    public boolean contains(EObject eObject);

