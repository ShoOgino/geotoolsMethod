    private static void assertWKT(String wkt) throws ParseException {
        Geometry g = DataBuilder.toGeometry(wkt);
        String s = WKTAdapter.DEFAULT.toWKT(g);
        assertTrue("WKT: [" + s + "] is not equal to [" + wkt + "]", wkt.equals(s));
    }

