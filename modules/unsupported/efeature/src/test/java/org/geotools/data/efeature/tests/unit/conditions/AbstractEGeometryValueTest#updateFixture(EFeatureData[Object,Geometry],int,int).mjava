    @Override
    protected final void updateFixture(
            EFeatureData<Object, Geometry> eFixture, int value, int pass) {
        //
        // Update geometry value
        //
        eFixture.setGeometry((Geometry) getTest(value, pass));
    }

