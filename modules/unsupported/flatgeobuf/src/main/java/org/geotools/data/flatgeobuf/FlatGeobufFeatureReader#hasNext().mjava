    @Override
    public boolean hasNext() throws IOException {
        if (nextFeature != null) {
            return true;
        } else {
            if (isEmpty) {
                return false;
            }
            if (it.hasNext()) {
                nextFeature = it.next();
                return true;
            } else {
                return false;
            }
        }
    }

