    @Override
    public boolean hasNext() throws IOException {
        if (this.writer == null) {
            return false;
        }
        if (this.appending) {
            return false;
        }
        return delegate.hasNext();
    }

