    /**
     * Close the internal reader accessing the file.
     * 
     * @throws IOException
     */
    public void close() throws IOException {
        reader.close();
        reader = null;
    }

