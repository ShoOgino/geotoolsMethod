    @Override
    public int hashCode() {
        return new HashCodeBuilder().append(supportsPagination).append(supportsQueryRelatedPagination).append(supportsQueryWithDistance).append(supportsReturningQueryExtent).append(supportsStatistics).append(supportsOrderBy).append(supportsDistinct).append(supportsQueryWithResultType).append(supportsSqlExpression).append(supportsAdvancedQueryRelated).append(supportsReturningGeometryCentroid).toHashCode();
    }

