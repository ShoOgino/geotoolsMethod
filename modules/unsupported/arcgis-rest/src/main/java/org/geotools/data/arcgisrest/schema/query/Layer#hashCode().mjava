    @Override
    public int hashCode() {
        return new HashCodeBuilder()
                .append(objectIdFieldName)
                .append(globalIdFieldName)
                .append(geometryType)
                .append(spatialReference)
                .append(fields)
                .append(features)
                .toHashCode();
    }

