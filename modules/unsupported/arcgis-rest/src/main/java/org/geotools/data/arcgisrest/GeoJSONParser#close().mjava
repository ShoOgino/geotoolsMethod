    /**
     * Closes associated resources (such as the inpout stream)
     */
    @Override
    public void close() {
        try {
            this.reader.close();
        } catch (IOException e) {
            LOGGER.log(Level.SEVERE, e.getMessage(), e);
        }
    }

