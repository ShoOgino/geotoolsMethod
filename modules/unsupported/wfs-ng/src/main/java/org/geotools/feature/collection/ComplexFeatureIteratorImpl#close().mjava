    @Override
    public void close() {
        try {
            parser.close();
            parsedFeature = null;
        } catch (IOException e) {
            LOGGER.log(Level.FINER, e.getMessage(), e);
        }
    }

