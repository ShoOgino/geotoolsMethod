    /**
     * @param line
     * @return
     */
    protected String getXY(String line) {
        String coord = "";
        coord = getValue(line, coordDesc);
        return coord.replace("+", "").replaceAll(";", " ");
    }

