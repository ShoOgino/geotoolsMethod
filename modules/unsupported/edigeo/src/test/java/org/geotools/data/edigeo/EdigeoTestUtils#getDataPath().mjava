    /**

     *

     */
    private static File getDataPath() {
        try {
            return TestData.file(EdigeoTestUtils.class, null);
        } catch (IOException e) {
            return null;
        }
    }

