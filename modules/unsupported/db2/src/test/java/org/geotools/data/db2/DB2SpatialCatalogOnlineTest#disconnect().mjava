    /**
     * Closes the database connection before we terminate.
     *
     * @throws Exception
     */
    protected void disconnect() throws Exception {
        conn.close();
        super.disconnect();
    }

