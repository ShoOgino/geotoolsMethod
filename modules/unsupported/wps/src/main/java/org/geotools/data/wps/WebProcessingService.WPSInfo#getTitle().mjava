        @Override
        public String getTitle() {
            if (serverURL == null) {
                return "Unavailable";
            } else {
                return serverURL.toString();
            }
        }

