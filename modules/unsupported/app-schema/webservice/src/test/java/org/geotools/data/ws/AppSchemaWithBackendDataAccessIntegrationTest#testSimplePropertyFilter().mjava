    public void testSimplePropertyFilter() throws Exception {

        // Filter translates correctly for this case. However the test fails when mapping the
        // property
        // gsml:purpose. A class cast exception is thrown in
        // DataAccessMappingFeatureIterator.setAttributeValue,
        // line 298. The line is:
        // valueList.add((Property) singleVal);
        // singleVal is a string with the value CONSTANT.
        final Filter filter = ff.equals(ff.property("gml:name/@codeSpace"), ff
                .literal("gsv:NameSpace"));
        runAppSchemaBackendTests(filter);
    }

