    /**
     * Returns the Bounded by domain.
     */
    public  BoundedBy getBoundedBy() {
        if (boundedBy == null) {
            boundedBy = new BoundedBy(this);
        }
        return boundedBy;
    }

