    @Override
    protected void setUpInternal() throws Exception {
        configFieldsSetup();
        createTestFolder();
        client = new HttpSolrClient.Builder(getSolrCoreURL()).build();
        solrDataSetup();
        prepareFiles();
        setupDataStore();
    }

