    /**
     * Returns the width of this grid. All {@linkplain PointArray point array} in this grid must
     * have this length.
     *
     * @return The grid width.
     * @see PointArray#size
     */
    public int width() {
        return pointGrid[0].size();
    }

