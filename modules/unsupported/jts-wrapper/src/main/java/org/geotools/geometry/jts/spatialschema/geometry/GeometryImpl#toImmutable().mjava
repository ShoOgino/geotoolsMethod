    /**
     * Creates an immutable copy of this object or just returns this object if it's already
     * immutable.
     */
    public final Geometry toImmutable() {
        if (isMutable()) {
            GeometryImpl result = (GeometryImpl) clone();
            result.mutable = false;
            return result;
        } else {
            return this;
        }
    }

