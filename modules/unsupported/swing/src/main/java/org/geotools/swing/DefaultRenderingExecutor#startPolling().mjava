    private void startPolling() {
        watcher =
                watchExecutor.scheduleAtFixedRate(
                        () -> pollTaskResult(),
                        pollingInterval,
                        pollingInterval,
                        TimeUnit.MILLISECONDS);
    }

