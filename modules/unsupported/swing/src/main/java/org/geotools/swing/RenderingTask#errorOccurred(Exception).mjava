    /**
     * Called by the renderer on error
     *
     * @param e cause of the error
     */
    public void errorOccurred(Exception e) {
        running.set(false);
        failed.set(true);
    }

