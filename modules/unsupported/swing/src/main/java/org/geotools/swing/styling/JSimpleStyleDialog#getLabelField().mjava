    /**
     * Get the feature field (attribute) to use for labels
     *
     * @return field name
     */
    public String getLabelField() {
        if (labelFeatures) {
            return labelField;
        }

        return null;
    }

