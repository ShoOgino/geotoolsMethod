    /**
     * Cancels this rendering task. May be called prior to the rendering
     * task starting.
     */
    @Override
    public void cancel() {
        if (running.get()) {
            renderer.stopRendering();
        }
        
        cancelled.set(true);
    }

