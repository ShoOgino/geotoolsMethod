    /**
     * Validation of page state; if the page isValid then the next or finish button will be enabled.
     *
     * <p>If you need to call setNextPageIdentifier based on input this is the time to do it.
     *
     * @return true if the page contents are valid
     */
    public boolean isValid() {
        return true;
    }

