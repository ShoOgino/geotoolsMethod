    /**
     * Notifies that the operation begins. This method display the windows if it was not already
     * visible.
     */
    @Override
    public void started() {
        call(Caller.STARTED);
    }

