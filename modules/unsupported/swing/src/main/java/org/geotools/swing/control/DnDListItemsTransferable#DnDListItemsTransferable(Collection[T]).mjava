    /**
     * Constructor
     *
     * @param stuff a Collection of list item data
     */
    public DnDListItemsTransferable(Collection<T> stuff) {
        super();
        items = new ArrayList<>();
        items.addAll(stuff);
    }

