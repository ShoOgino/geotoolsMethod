        @Override
        public void onCancel() {
            CountDownLatch latch = latches[EventType.CANCEL.ordinal()];
            if (latch != null) {
                latch.countDown();
            }
        }

