    @Override
    public String[] getMetadataNames() {
        if (raster.time != null) {
            return new String[] {
                HAS_TIME_DOMAIN, TIME_DOMAIN, TIME_DOMAIN_MINIMUM, TIME_DOMAIN_MAXIMUM
            };
        }
        return null;
    }

