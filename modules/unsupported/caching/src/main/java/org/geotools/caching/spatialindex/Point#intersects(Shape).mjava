    //
    // IShape interface
    //
    public boolean intersects(final Shape s) {
        if (s instanceof Region) {
            return ((Region) s).contains(this);
        }

        return false;
    }

