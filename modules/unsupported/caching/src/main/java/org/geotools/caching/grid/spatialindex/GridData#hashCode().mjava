    public int hashCode() {
        if (hashCode == 0) {
            // int hash = 17;
            //  hash = (37 * hash) + shape.hashCode();
            int hash = 629 + shape.hashCode(); // 629 = 17 * 37
            hash = (37 * hash) + data.hashCode();
            this.hashCode = hash;
        }
        return hashCode;
    }

