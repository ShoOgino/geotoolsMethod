        /**
         * Returns the nodes that have been write locked. These are the nodes that are currently
         * being written to.
         *
         * @return
         */
        public Collection<NodeIdentifier> getMissingNodes() {
            return this.writelocks;
        }

