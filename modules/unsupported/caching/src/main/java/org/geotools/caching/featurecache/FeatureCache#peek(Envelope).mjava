    /**
     * Looks in the cache for the features within a given envelope.
     *
     * <p>This function will only look in the cache for the features; it will not look in the
     * underlying datastore.
     *
     * @param e
     * @return Collection of features found in the cache.
     */
    public SimpleFeatureCollection peek(Envelope e);

