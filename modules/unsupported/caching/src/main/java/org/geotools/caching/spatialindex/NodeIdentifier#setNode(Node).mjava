    /**
     * Sets the node associated with the nodeid; this node is stored as a soft reference so
     * getNode() may return null.
     *
     * @param n
     */
    public void setNode(Node n) {
        this.node = new SoftReference<Node>(n);
    }

