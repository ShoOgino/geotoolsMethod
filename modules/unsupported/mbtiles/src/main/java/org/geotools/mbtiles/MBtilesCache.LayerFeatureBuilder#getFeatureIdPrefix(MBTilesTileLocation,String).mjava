        private String getFeatureIdPrefix(MBTilesTileLocation tile, String typeName) {
            return typeName
                    + "_"
                    + tile.getZoomLevel()
                    + "_"
                    + tile.getTileRow()
                    + "_"
                    + tile.getTileColumn()
                    + ".";
        }

