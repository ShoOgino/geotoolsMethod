    public void forEach(PositionConsumer consumer) {
        for (long y = minY; y <= maxY; y++) {
            for (long x = minX; x <= maxX; x++) {
                consumer.accept(x, y);
            }
        }
    }

