        /**
         * @param fs
         * @param query
         */
        public WFSFeatureResults( WFSFeatureSource fs, Query query ) throws IOException {
            super(fs, query);
            this.query = query;
            this.fs = fs;
        }

