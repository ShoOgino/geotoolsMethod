    public void testQueryCapabilitiesReliableFids() throws Exception {
        FeatureSource fs = ds.getFeatureSource( table8 );
        assertFalse( fs.getQueryCapabilities().isReliableFIDSupported() );
        
        fs = ds.getFeatureSource( table1 );
        assertTrue( fs.getQueryCapabilities().isReliableFIDSupported() );
    }

