    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetGeodeticDatum(GeodeticDatumType newGeodeticDatum, NotificationChain msgs) {
        GeodeticDatumType oldGeodeticDatum = geodeticDatum;
        geodeticDatum = newGeodeticDatum;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.GEODETIC_DATUM_REF_TYPE__GEODETIC_DATUM, oldGeodeticDatum, newGeodeticDatum);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

