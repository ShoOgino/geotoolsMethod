    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetVerticalDatum(VerticalDatumType newVerticalDatum, NotificationChain msgs) {
        VerticalDatumType oldVerticalDatum = verticalDatum;
        verticalDatum = newVerticalDatum;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.VERTICAL_DATUM_REF_TYPE__VERTICAL_DATUM, oldVerticalDatum, newVerticalDatum);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

