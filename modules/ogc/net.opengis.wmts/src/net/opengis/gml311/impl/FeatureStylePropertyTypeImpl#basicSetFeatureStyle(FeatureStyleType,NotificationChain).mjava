    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetFeatureStyle(FeatureStyleType newFeatureStyle, NotificationChain msgs) {
        FeatureStyleType oldFeatureStyle = featureStyle;
        featureStyle = newFeatureStyle;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.FEATURE_STYLE_PROPERTY_TYPE__FEATURE_STYLE, oldFeatureStyle, newFeatureStyle);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

