    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetMultiPolygon(MultiPolygonType newMultiPolygon, NotificationChain msgs) {
        MultiPolygonType oldMultiPolygon = multiPolygon;
        multiPolygon = newMultiPolygon;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.MULTI_POLYGON_PROPERTY_TYPE__MULTI_POLYGON, oldMultiPolygon, newMultiPolygon);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

