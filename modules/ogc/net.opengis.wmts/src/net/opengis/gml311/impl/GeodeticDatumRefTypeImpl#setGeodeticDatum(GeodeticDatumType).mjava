    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    @Override
    public void setGeodeticDatum(GeodeticDatumType newGeodeticDatum) {
        if (newGeodeticDatum != geodeticDatum) {
            NotificationChain msgs = null;
            if (geodeticDatum != null)
                msgs = ((InternalEObject)geodeticDatum).eInverseRemove(this, EOPPOSITE_FEATURE_BASE - Gml311Package.GEODETIC_DATUM_REF_TYPE__GEODETIC_DATUM, null, msgs);
            if (newGeodeticDatum != null)
                msgs = ((InternalEObject)newGeodeticDatum).eInverseAdd(this, EOPPOSITE_FEATURE_BASE - Gml311Package.GEODETIC_DATUM_REF_TYPE__GEODETIC_DATUM, null, msgs);
            msgs = basicSetGeodeticDatum(newGeodeticDatum, msgs);
            if (msgs != null) msgs.dispatch();
        }
        else if (eNotificationRequired())
            eNotify(new ENotificationImpl(this, Notification.SET, Gml311Package.GEODETIC_DATUM_REF_TYPE__GEODETIC_DATUM, newGeodeticDatum, newGeodeticDatum));
    }

