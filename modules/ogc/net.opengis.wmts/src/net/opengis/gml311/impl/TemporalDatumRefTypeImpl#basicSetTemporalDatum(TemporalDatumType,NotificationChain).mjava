    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetTemporalDatum(TemporalDatumType newTemporalDatum, NotificationChain msgs) {
        TemporalDatumType oldTemporalDatum = temporalDatum;
        temporalDatum = newTemporalDatum;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.TEMPORAL_DATUM_REF_TYPE__TEMPORAL_DATUM, oldTemporalDatum, newTemporalDatum);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

