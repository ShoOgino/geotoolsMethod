    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetGraphStyle(GraphStyleType newGraphStyle, NotificationChain msgs) {
        GraphStyleType oldGraphStyle = graphStyle;
        graphStyle = newGraphStyle;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.GRAPH_STYLE_PROPERTY_TYPE__GRAPH_STYLE, oldGraphStyle, newGraphStyle);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

