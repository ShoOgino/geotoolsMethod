    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public void setTemporalDatum(TemporalDatumType newTemporalDatum) {
        if (newTemporalDatum != temporalDatum) {
            NotificationChain msgs = null;
            if (temporalDatum != null)
                msgs = ((InternalEObject)temporalDatum).eInverseRemove(this, EOPPOSITE_FEATURE_BASE - Gml311Package.TEMPORAL_DATUM_REF_TYPE__TEMPORAL_DATUM, null, msgs);
            if (newTemporalDatum != null)
                msgs = ((InternalEObject)newTemporalDatum).eInverseAdd(this, EOPPOSITE_FEATURE_BASE - Gml311Package.TEMPORAL_DATUM_REF_TYPE__TEMPORAL_DATUM, null, msgs);
            msgs = basicSetTemporalDatum(newTemporalDatum, msgs);
            if (msgs != null) msgs.dispatch();
        }
        else if (eNotificationRequired())
            eNotify(new ENotificationImpl(this, Notification.SET, Gml311Package.TEMPORAL_DATUM_REF_TYPE__TEMPORAL_DATUM, newTemporalDatum, newTemporalDatum));
    }

