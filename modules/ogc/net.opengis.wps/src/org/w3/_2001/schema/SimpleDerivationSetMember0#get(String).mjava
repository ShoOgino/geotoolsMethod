	/**
	 * Returns the '<em><b>Simple Derivation Set Member0</b></em>' literal with the specified literal value.
	 * <!-- begin-user-doc -->
	 * <!-- end-user-doc -->
	 * @param literal the literal.
	 * @return the matching enumerator or <code>null</code>.
	 * @generated
	 */
	public static SimpleDerivationSetMember0 get(String literal) {
		for (int i = 0; i < VALUES_ARRAY.length; ++i) {
			SimpleDerivationSetMember0 result = VALUES_ARRAY[i];
			if (result.toString().equals(literal)) {
				return result;
			}
		}
		return null;
	}

