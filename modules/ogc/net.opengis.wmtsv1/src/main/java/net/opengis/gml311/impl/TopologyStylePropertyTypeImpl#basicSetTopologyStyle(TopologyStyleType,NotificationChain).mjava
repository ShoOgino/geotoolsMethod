    /**
     * <!-- begin-user-doc -->
     * <!-- end-user-doc -->
     * @generated
     */
    public NotificationChain basicSetTopologyStyle(TopologyStyleType newTopologyStyle, NotificationChain msgs) {
        TopologyStyleType oldTopologyStyle = topologyStyle;
        topologyStyle = newTopologyStyle;
        if (eNotificationRequired()) {
            ENotificationImpl notification = new ENotificationImpl(this, Notification.SET, Gml311Package.TOPOLOGY_STYLE_PROPERTY_TYPE__TOPOLOGY_STYLE, oldTopologyStyle, newTopologyStyle);
            if (msgs == null) msgs = notification; else msgs.add(notification);
        }
        return msgs;
    }

