    @Override
    public Operator getOperator(String name) {
        for (Operator op : getOperators()){
            if (op.getName().equals(name)){
                return op;
            }
        }
        return null;
    }

