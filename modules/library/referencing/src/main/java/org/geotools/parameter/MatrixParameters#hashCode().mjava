    @Override
    public int hashCode() {
        int result = Objects.hash(super.hashCode(), numRow, numCol);
        result = 31 * result + Arrays.hashCode(matrixValues);
        return result;
    }

