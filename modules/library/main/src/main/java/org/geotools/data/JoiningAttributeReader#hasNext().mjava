    @Override
    public boolean hasNext() throws IOException {
        for (AttributeReader reader : readers) {
            if (reader.hasNext()) {
                return true;
            }
        }

        return false;
    }

