    public boolean hasNext() throws IOException {
        for (AttributeWriter writer : writers) {
            if (writer.hasNext()) {
                return true;
            }
        }
        return false;
    }

