    @Override
    float[] rescale(float[] values) {
        if (defaultUnit == null || defaultUnit == NonSI.PIXEL) {
            return super.rescale(values);
        } else {
            return values;
        }        
    }

