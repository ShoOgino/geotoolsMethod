    private byte[] getByteBuffer(int length) {
        byte[] buffer;
        if (length < this.buffer.length) {
            buffer = this.buffer;
        } else {
            buffer = new byte[length];
        }
        return buffer;
    }

