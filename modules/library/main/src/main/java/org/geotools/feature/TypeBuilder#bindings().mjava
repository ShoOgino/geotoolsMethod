    /** Accessor for bindings. */
    protected Map<Class<?>, AttributeType> bindings() {
        if (bindings == null) {
            bindings = new HashMap<>();
        }
        return bindings;
    }

