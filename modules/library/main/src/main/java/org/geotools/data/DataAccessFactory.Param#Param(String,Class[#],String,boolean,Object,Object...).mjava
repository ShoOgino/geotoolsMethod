        public Param(
                String key,
                Class<?> type,
                String description,
                boolean required,
                Object sample,
                Object... metadata) {
            this(key, type, description, required, sample, new KVP(metadata));
        }

