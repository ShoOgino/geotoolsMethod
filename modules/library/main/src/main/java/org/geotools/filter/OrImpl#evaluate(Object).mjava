    @Override
    public boolean evaluate(Object feature) {
        for (Filter filter : children) {
            if (filter.evaluate(feature)) {
                return true;
            }
        }
        return false;
    }

