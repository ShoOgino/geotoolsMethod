    /** @see org.geotools.data.FeatureReader#close() */
    @Override
    public void close() throws IOException {
        closed = true;

        if (features != null) {
            features.close();
            features = null;
        }
    }

