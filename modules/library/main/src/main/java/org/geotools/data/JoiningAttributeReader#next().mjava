    @Override
    public void next() throws IOException {
        for (AttributeReader reader : readers) {
            if (reader.hasNext()) {
                reader.next();
            }
        }
    }

