        void dispose() {
            if (ref != null) {
                DataStore store = ref.get();
                if (store != null) store.dispose();
                ref.clear();
            }
        }

