    /**
     * Moves the IO to the specified offset in the file
     *
     * @param offset
     * @throws IOException
     */
    public void seek(long offset) throws IOException {
        raf.seek(offset);
    }

