    public CoordinateWriter(
            int numDecimals,
            String tupleDelim,
            String coordDelim,
            boolean useDummyZ,
            double zValue) {
        this(numDecimals, tupleDelim, coordDelim, useDummyZ, 0, 2);
    }

