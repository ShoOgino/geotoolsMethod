    /**
     * Does a quick walk to detect only a list of attribute names.
     *
     * <p>This method does not produce the complete schema (ie derrived restrictions based on
     * attribute facets). It is only used to get a list of the unique attribtues in the resulting
     * schema.
     *
     * @param featureType
     * @return Set of unique attribute names
     */
    public List getNames(SimpleFeatureType featureType) {
        return getNames(featureType, new ArrayList());
    }

