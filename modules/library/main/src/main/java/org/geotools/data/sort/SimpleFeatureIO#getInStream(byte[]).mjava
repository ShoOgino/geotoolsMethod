    private InStream getInStream(byte[] buffer) {
        if (bis == null) {
            bis = new ByteArrayInStream(buffer);
        } else {
            bis.setBytes(buffer);
        }
        return bis;
    }

