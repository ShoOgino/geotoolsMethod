    public SimpleFeature next() {
        if (counter < start) {
            // skip to just before start if needed
            skip();
        }
        if (counter <= end) {
            counter++;
            SimpleFeature next = delegate.next();
            return next;
        }
        return null;
    }

