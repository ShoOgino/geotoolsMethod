    /**
     * Returns the constant {@linkplain #FIDS_HANDLE}.
     *
     * @return {@linkplain #FIDS_HANDLE}
     * @see Query#getHandle()
     */
    @Override
    public String getHandle() {
        return FIDS_HANDLE;
    }

