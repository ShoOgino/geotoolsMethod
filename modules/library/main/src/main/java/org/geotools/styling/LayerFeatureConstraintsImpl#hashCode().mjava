    public int hashCode() {
        final int PRIME = 1000003;
        int result = 0;

        if (constraints != null) {
            result = (PRIME * result) + Arrays.hashCode(constraints);
        }

        return result;
    }

