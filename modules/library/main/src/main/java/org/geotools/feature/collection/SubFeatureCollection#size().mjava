    @Override
    public int size() {
        int count = 0;
        try (SimpleFeatureIterator i = features()) {
            while (i.hasNext()) {
                i.next();
                count++;
            }
        }
        return count;
    }

