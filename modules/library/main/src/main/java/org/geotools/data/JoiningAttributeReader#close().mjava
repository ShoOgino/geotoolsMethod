    @Override
    public void close() throws IOException {
        IOException dse = null;

        for (AttributeReader reader : readers) {
            try {
                reader.close();
            } catch (IOException e) {
                dse = e;
            }
        }

        if (dse != null) {
            throw dse;
        }
    }

