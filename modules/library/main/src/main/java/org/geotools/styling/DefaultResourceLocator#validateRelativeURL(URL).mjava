    protected URL validateRelativeURL(URL relativeUrl) {
        File f;
        f = DataUtilities.urlToFile(relativeUrl);
        if (f.exists()) {
            // bingo!
            return relativeUrl;
        }
        return null;
    }

