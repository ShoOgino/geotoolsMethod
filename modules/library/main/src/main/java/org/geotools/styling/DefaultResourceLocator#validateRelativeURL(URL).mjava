    protected URL validateRelativeURL(URL relativeUrl) {
        File f = URLs.urlToFile(relativeUrl);
        if (f != null && f.exists()) {
            // bingo!
            return relativeUrl;
        }
        return null;
    }

