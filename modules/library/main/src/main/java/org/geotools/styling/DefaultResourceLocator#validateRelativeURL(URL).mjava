    protected URL validateRelativeURL(URL relativeUrl) {
        File f;
        f = URLs.urlToFile(relativeUrl);
        if (f.exists()) {
            // bingo!
            return relativeUrl;
        }
        return null;
    }

