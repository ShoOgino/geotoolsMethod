    /** Removes the given key from the cache. */
    @Override
    public void remove(final Object key) {
        synchronized (locks) {
            locks.remove(key);
            cache.remove(key);
        }
    }

