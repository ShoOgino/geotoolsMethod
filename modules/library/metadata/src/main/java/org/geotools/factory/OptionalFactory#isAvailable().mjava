    /**
     * Returns {@code true} if this factory is ready for use. An optional factory may returns {@code
     * false} for now but returns {@code true} later. However, the converse is not recommended.
     */
    boolean isAvailable();

