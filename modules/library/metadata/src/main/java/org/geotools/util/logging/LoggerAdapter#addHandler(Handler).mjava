    /**
     * Do nothing since this logger adapter does not supports handlers. The configuration should be
     * fully controlled by the external logging framework (e.g. <a
     * href="http://commons.apache.org/logging/">Commons-logging</a>) instead, which is not expected
     * to use {@link Handler} objects.
     */
    @Override
    public void addHandler(Handler handler) {}

