    /**
     * Logs a record at the specified level. The default implementation discards the exception and
     * delegates to <code>{@linkplain #log(Level,String) log}(level, message)</code>.
     */
    @Override
    public void log(final Level level, final String message, final Throwable thrown) {
        log(level, message);
    }

