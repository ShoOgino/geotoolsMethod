    /**
     * Returns the separator character. Default to <code>':'</code>. This method is overridden by
     * {@link org.geotools.util.ScopedName}.
     */
    char getSeparator() {
        return DEFAULT_SEPARATOR;
    }

