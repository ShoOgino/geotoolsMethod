    boolean addAndCheck(Object item) {
        Set<Object> set = threadLocalSet.get();
        if (set == null) {
            set = new HashSet<>();
            threadLocalSet.set(set);
        }
        return set.add(item);
    }

