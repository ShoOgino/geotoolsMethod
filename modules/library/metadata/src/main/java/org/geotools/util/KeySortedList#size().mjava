    /** Returns the number of elements in this list. */
    @Override
    public int size() {
        int count = 0;
        for (final List<V> list : map.values()) {
            count += list.size();
        }
        return count;
    }

