    /**
     * Returns the level for {@link #entering}, {@link #exiting} and {@link #throwing} methods. The
     * default implementation returns {@link Level#FINER}, which is consistent with the value used
     * in the Java logging framework. Subclasses should override this method if a different debug
     * level is wanted.
     */
    protected Level getDebugLevel() {
        return Level.FINER;
    }

