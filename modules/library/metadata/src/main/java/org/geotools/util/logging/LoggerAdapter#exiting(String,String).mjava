    /**
     * Logs a method return to the {@linkplain #getDebugLevel debug level}. Compared to the default
     * {@link Logger}, this implementation bypass the level check in order to let the backing
     * logging framework do its own check.
     */
    @Override
    public void exiting(final String sourceClass, final String sourceMethod) {
        logp(getDebugLevel(), sourceClass, sourceMethod, "RETURN");
    }

