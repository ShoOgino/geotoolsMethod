        /**
         * Constructs an initially empty set of statistics. All statistical values are initialized
         * to {@link Double#NaN}.
         */
        public Delta() {
            delta = new Statistics();
            delta.nNaN = -1; // Do not count the first NaN, which will always be the first value.
        }

