        /**
         * Replaces the last element returned by {@link #next} or {@link #previous} with the
         * specified element.
         */
        @Override
        public void set(final V o) {
            valuesIter.set(o);
        }

