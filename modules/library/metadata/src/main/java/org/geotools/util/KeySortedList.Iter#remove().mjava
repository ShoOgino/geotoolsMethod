        /**
         * Removes from the list the last element that was returned by {@link #next} or {@link
         * #previous}
         */
        @Override
        public void remove() {
            valuesIter.remove();
        }

