        @Override
        public boolean hasNext() {
            if(sources.isEmpty() && !residualInDegrees.isEmpty()) {
                throwLoopException();
            }
            return !sources.isEmpty();
        }

