    /**
     * Returns the number of {@link Double#NaN NaN} samples. {@code NaN} samples are ignored in all
     * other statitical computation. This method count them for information purpose only.
     */
    public int countNaN() {
        return Math.max(nNaN, 0);
    }

