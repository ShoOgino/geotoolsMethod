    /** Stores a value */
    public void put(final K key, final V object) {
        if (cache.size() < LIMIT) {
            writeLock(key);
            cache.put(key, object);
            writeUnLock(key);
        }
    }

