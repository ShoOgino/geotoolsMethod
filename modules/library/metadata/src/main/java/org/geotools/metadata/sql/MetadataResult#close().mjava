    /**
     * Close this statement and free all resources. After this method has been invoked, this object
     * can't be used anymore.
     *
     * @throws SQLException if an SQL operation failed.
     */
    @Override
    public void close() throws SQLException {
        if (results != null) {
            results.close();
            results = null;
        }
        statement.close();
    }

