    /**
     * Sets the logger level for the warnings eventually emitted by the {@link #parse} method. The
     * default value is {@link Level#WARNING}.
     *
     * @param level The new logging level.
     * @since 2.5
     */
    public void setLevel(final Level level) {
        if (level != null) {
            this.level = level;
        }
    }

