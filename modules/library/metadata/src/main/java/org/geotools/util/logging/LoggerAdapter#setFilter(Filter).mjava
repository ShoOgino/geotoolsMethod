    /**
     * Do nothing since this logger adapter does not support filters. It is difficult to query
     * efficiently the filter in this {@code LoggerAdapter} architecture (e.g. we would need to make
     * sure that {@link Filter#isLoggable} is invoked only once even if a {@code log} call is
     * cascaded into many other {@code log} calls, and this test must works in multi-threads
     * environment).
     */
    @Override
    public void setFilter(Filter filter) {}

