    /**
     * Computes a hash code value for this metadata using Java reflection. The hash code is defined
     * as the sum of hash code values of all non-null properties. This is the same contract than
     * {@link java.util.Set#hashCode} and ensure that the hash code value is insensitive to the
     * ordering of properties.
     */
    @Override
    public int hashCode() {
        return getStandard().hashCode(this);
    }

