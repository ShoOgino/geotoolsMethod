    /**
     * Returns {@code true} if the specified array contains at least one {@link Float#NaN NaN}
     * value.
     */
    public static boolean hasNaN(final float[] array) {
        for (int i = 0; i < array.length; i++) {
            if (Float.isNaN(array[i])) {
                return true;
            }
        }
        return false;
    }

