    /**
     * Returns a hash value for this factory. The default implementation computes the hash value
     * using only immutable properties. This computation do <strong>not</strong> relies on
     * {@linkplain #getImplementationHints implementation hints}, since there is no garantee that
     * they will not change.
     *
     * @since 2.3
     */
    @Override
    public final int hashCode() {
        return getClass().hashCode() + (37 * priority);
    }

