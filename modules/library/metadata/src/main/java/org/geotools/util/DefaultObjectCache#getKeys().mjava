    /**
     * Retrieves all keys currently in the cache.
     *
     * @return Set of keys
     */
    public Set<K> getKeys() {
        synchronized (cache) {
            return new HashSet<>(cache.keySet());
        }
    }

