    @Override
    public int hashCode() {
        final ByteBuffer byteBuffer = get();
        if (byteBuffer != null) {
            return byteBuffer.hashCode();
        }
        return super.hashCode();
    }

