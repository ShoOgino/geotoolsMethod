    /**
     * Returns {@code true} if the specified array contains at least one {@link Double#NaN NaN}
     * value.
     */
    public static boolean hasNaN(final double[] array) {
        for (double v : array) {
            if (Double.isNaN(v)) {
                return true;
            }
        }
        return false;
    }

