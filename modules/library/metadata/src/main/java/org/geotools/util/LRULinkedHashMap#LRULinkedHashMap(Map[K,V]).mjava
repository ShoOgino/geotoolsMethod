    /**
     * Constructs a {@code LRULinkedHashMap} with all entries from the specified map. The
     * {@linkplain #getMaximumSize maximum size} is set to the given {@linkplain Map#size map size}.
     *
     * @param map The map whose mappings are to be placed in this map.
     * @since 2.5
     */
    public LRULinkedHashMap(final Map<K, V> map) {
        super(map);
        maximumSize = map.size();
    }

