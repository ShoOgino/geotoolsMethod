    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropAbcTable();
        } catch (SQLException e) {
        }

        // create all the data
        createAbcTable();
    }

