    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropSkipColumnTable();
        } catch (SQLException e) {
        }

        // create all the data
        createSkipColumnTable();
    }

