    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropLobTable();
        } catch (SQLException e) {
        }

        // create all the data
        createLobTable();
    }

