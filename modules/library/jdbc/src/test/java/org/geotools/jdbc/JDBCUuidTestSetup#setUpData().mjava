    @Override
    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropUuidTable();
        } catch (SQLException e) {
        }

        // create all the data
        createUuidTable();
    }

