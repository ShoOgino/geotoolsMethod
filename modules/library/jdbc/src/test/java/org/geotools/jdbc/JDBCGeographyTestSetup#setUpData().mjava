    protected final void setUpData() throws Exception {
        //kill all the data
        try {
            dropGeoPointTable();
        } catch (SQLException e) {
        }

        //create all the data
        if(isGeographySupportAvailable()) {
            createGeoPointTable();
        }
    }

