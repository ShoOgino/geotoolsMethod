    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropGeoPointTable();
            dropGeoLineTable();
        } catch (SQLException e) {
        }

        // create all the data
        if (isGeographySupportAvailable()) {
            createGeoPointTable();
            createGeoLineTable();
        }
    }

