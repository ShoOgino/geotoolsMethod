    @Override
    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropBooleanTable();
        } catch (SQLException e) {
        }

        // create all the data
        createBooleanTable();
    }

