    @Override
    public void setUp() throws Exception {
        super.setUp();
        
        try {
            dropLakeTable();
        } catch (SQLException e) {
        }

        //create all the data
        createLakeTable();
    }

