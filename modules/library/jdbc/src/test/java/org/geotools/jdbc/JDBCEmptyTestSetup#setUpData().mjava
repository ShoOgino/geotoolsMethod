    @Override
    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropEmptyTable();
        } catch (SQLException e) {
        }

        // create all the data
        createEmptyTable();
    }

