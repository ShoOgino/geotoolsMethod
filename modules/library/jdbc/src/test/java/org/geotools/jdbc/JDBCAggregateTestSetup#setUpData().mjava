    protected final void setUpData() throws Exception {
        super.setUpData();
        //kill all the data
        try {
            dropAggregateTable();
        } catch (SQLException e) {
        }

        //create all the data
        createAggregateTable();
    }

