    protected final void setUpData() throws Exception {
        delegate.setUpData();

        // kill all the data
        try {
            dropTypes();
        } catch (SQLException e) {
        }

        // create all the data
        createTypes();
    }

