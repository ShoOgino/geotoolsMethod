    @Override
    protected void setUpData() throws Exception {
        try {
            dropLakeTable();
        } catch (SQLException e) {
        }

        // create all the data
        createLakeTable();
    }

