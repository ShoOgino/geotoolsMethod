    @Override
    protected final void setUpData() throws Exception {
        // kill all the data
        try {
            dropPersonTable();
        } catch (SQLException e) {
        }

        try {
            dropZipCodeTable();
        } catch (SQLException e) {
        }

        // create all the data
        createPersonTable();
    }

