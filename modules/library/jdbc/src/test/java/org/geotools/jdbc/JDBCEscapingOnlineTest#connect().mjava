    @Override
    protected void connect() throws Exception {
        super.connect();
        escapingSchema =
                DataUtilities.createType(
                        dataStore.getNamespaceURI() + "." + ESCAPING,
                        ID + ":0," + NAME + ":String");
    }

