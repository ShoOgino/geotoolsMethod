    @Override
    @SuppressWarnings("unchecked")
    public <T> T unwrap(Class<T> c) throws SQLException {
        if (isWrapperFor(c)) {
            return (T) getWrapped();
        }
        return null;
    }

