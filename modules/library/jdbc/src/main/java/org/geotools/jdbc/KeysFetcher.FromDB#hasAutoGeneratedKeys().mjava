        @Override
        public boolean hasAutoGeneratedKeys() {
            for (KeyFetcher fetcher : fetchers) {
                if(fetcher.isAutoGenerated()) {
                    return true;
                }
            }
            return false;
        }

