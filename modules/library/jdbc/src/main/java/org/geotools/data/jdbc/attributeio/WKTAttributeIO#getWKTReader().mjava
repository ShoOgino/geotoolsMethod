    /**
     * Lazily initialize the WKTReader
     *
     */
    private WKTReader getWKTReader() {
        if (reader == null) {
            reader = new WKTReader2();
        }

        return reader;
    }

