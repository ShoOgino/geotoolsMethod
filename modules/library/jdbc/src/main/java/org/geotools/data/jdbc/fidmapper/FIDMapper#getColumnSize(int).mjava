    /**
     * Returns the size of a primary key column as it would be provided by the database metadata.
     * Some fields requires a size specification, such as VARCHAR or NUMBER, whilst other don't have
     * or don't need it (for example, an INTEGER or a TEXT field).
     *
     * @param colIndex
     */
    public int getColumnSize(int colIndex);

