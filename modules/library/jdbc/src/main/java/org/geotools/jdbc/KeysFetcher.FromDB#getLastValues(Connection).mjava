        private List<Object> getLastValues(Connection cx) throws SQLException {
            List<Object> last = new ArrayList<>();
            for (KeyFetcher fetcher : fetchers) {
                last.add(fetcher.getLastValue(cx));
            }
            return last;
        }

