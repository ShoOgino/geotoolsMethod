    /**
     * Creates the value for the PK attributes given the feature. If the FID is null, will throw an
     * IOException if not possible. If null is returned, no primary key value needs to be specified,
     * which is what we want for auto-increment fields.
     *
     * @param FID The feature ID is going to be parsed
     * @throws IOException
     */
    public Object[] getPKAttributes(String FID) throws IOException;

