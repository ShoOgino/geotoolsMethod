    private Map<String, SimpleFeature> getFeatureMapSync(String typeName) {
        if (typeName == null) {
            return null;
        }
    
        synchronized (memory) {
            return memory.get(typeName);
        }
    
    }

