    @Override
    public boolean drawImage(Image img, AffineTransform xform, ImageObserver obs) {
        this.image = img;
        this.transform = xform;
        this.observer = obs;
        return true;
    }

