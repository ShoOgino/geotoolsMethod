    /**
     * Returns the distance at which the offset curve is generated from the basis curve. In a 2D
     * system, positive distances are to be left of the basis curve, and negative distances are
     * right of the basis curve.
     *
     * @unitof Length
     */
    @UML(identifier = "distance", obligation = MANDATORY, specification = ISO_19107)
    double getDistance();

