    /**
     * The bounds of the attribute.
     *
     * <p>This value should be derived unless explicitly set via {@link #setBounds(BoundingBox)}.
     *
     * <p>In the case that the underlying geometry is <code>null</code>, this method should return
     * an empty bounds as opposed to returning <code>null</code>.
     *
     * @return The bounds of the underlying geometry, possibly empty.
     */
    BoundingBox getBounds();

