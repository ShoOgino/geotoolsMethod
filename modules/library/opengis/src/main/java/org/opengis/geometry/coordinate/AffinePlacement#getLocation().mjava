    /**
     * Gives the target of the parameter space origin. This is the vector (<var>x</var><sub>0</sub>,
     * <var>y</var><sub>0</sub>, <var>z</var><sub>0</sub>) in the formulae in the class description.
     */
    @UML(identifier = "location", obligation = MANDATORY, specification = ISO_19107)
    Position getLocation();

