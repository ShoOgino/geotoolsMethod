    /**
     * Returns the row at the given index. The row is backed by this {@code PointGrid}, so changes
     * to the row are reflected in the grid, and vice-versa.
     *
     * @param row The index from 0 inclusive to {@link #height} exclusive.
     * @return The row at the given index.
     * @throws IndexOutOfBoundsException if the index is out of bounds.
     */
    @Extension
    PointArray getRow(int row) throws IndexOutOfBoundsException;

