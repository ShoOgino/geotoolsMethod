    /**
     * Provides the minimum ordinate along the second axis. This is equivalent to <code>
     * {@linkplain #getMinimum getMinimum}(1)</code>. There is no guarantee that this axis is
     * oriented toward {@linkplain AxisDirection#NORTH North}.
     *
     * @return The minimum ordinate along the second axis.
     */
    double getMinY();

