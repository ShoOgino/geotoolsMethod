    /**
     * Creates an association type.
     *
     * @param name The name of the type.
     * @param relatedType The type of attributes referenced by the association.
     * @param isAbstract Flag indicating if the type is abstract.
     * @param restrictions Set of restrictions on the association.
     * @param superType Parent type.
     * @param description A description of the type..
     */
    AssociationType createAssociationType(
            Name name,
            AttributeType relatedType,
            boolean isAbstract,
            List<Filter> restrictions,
            AssociationType superType,
            InternationalString description);

