    /**
     * links this calendar to the {@linkplain CalendarEra calendar eras} that it uses as a reference
     * for dating.
     *
     * @todo The original version of this class returned {@code TemporalCalendarEra}, which doesn't
     *     exists in the provided sources. I assumed that it was a typo and that the actual class
     *     was {@link CalendarEra}.
     */
    @UML(identifier = "Basis", specification = ISO_19108)
    Collection<CalendarEra> getBasis();

