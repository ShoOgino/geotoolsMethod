    /**
     * Indicate property to sort by, specification is limited to PropertyName.
     *
     * <p>Not sure if this is allowed to be a xPath expression?
     *
     * <ul>
     *   <li>It would be consist with our use in GeoTools
     *   <li>It would not seem to agree with the short hand notation used by WFS1.1 (ie. "year A,
     *       month A, day A" )
     * </ul>
     *
     * @todo Use QName
     * @return Name of property to sort by.
     */
    PropertyName getPropertyName();

