    /**
     * Provides the maximum ordinate along the first axis. This is equivalent to <code>
     * {@linkplain #getMaximum getMaximum}(0)</code>. There is no guarantee that this axis is
     * oriented toward {@linkplain AxisDirection#EAST East}.
     *
     * @return The maximum ordinate along the first axis.
     */
    double getMaxX();

