    /**
     * Returns the set containing the elements that compose this {@code MultiPoint}. The set may be
     * modified if this geometry {@linkplain #isMutable is mutable}.
     *
     * @since GeoAPI 2.1
     */
    @UML(identifier = "element", obligation = MANDATORY, specification = ISO_19107)
    Set<Point> getElements();

