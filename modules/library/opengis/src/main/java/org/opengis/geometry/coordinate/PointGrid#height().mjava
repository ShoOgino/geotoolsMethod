    /**
     * Returns the length of this array. This is equivalent to <code>
     * {@linkplain #rows rows}().{@linkplain PointArray#length length}()</code>.
     *
     * @return The grid height.
     */
    @Extension
    int height();

