    /**
     * Constructs an enum with identical name and charset. This is needed for {@link
     * CodeList#valueOf} reflection.
     */
    private CharacterSet(final String name) {
        this(name, name);
    }

