    /**
     * Creates a geometric attribute type.
     *
     * @param name The name of the type.
     * @param binding The class of values of attributes of the type.
     * @param crs The coordinate reference system of the type.
     * @param isIdentifiable Flag indicating if the attribute is identifiable.
     * @param isAbstract Flag indicating if the type is abstract.
     * @param restrictions Set of restrictions on the attribute.
     * @param superType Parent type.
     * @param description A description of the type.
     */
    GeometryType createGeometryType(
            Name name,
            Class<?> binding,
            CoordinateReferenceSystem crs,
            boolean isIdentifiable,
            boolean isAbstract,
            List<Filter> restrictions,
            AttributeType superType,
            InternationalString description);

