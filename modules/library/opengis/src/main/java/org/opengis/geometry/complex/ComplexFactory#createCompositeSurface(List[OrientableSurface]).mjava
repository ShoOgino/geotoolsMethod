    /**
     * Creates a {@linkplain CompositeSurface composite surface} from a list of {@linkplain
     * OrientableSurface orientable surfaces}. The constructed composite surface is backed by the
     * given surface. That is, the composite surface holds references to the surface instances.
     *
     * @param generator a list of orientable surface.
     * @return a composite surface.
     */
    CompositeSurface createCompositeSurface(List<OrientableSurface> generator);

