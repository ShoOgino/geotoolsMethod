    /**
     * Constructs an enum with a default {@code isFloating} value. This is needed for {@link
     * CodeList#valueOf} reflection.
     */
    private PrecisionType(final String name) {
        this(name, true);
    }

