    public <E> List<E> toList(PartiallyOrderedSet<E> poset) {
        List<E> result = new ArrayList<E>();
        for (E e : poset) {
            result.add(e);
        }
        return result;
    }

