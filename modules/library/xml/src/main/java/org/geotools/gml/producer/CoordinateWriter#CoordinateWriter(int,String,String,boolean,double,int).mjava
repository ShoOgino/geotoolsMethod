    public CoordinateWriter(
            int numDecimals,
            String tupleDelim,
            String coordDelim,
            boolean useZ,
            double z,
            int dimension) {
        this(numDecimals, false, true, tupleDelim, coordDelim, useZ, z, dimension);
    }

