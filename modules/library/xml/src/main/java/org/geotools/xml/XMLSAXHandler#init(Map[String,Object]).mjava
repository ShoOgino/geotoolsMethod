    protected void init(Map<String, Object> hints) {
        if (hints == null) {
            hints = new HashMap<>();
        }
        this.hints = hints;
        setEntityResolver(XMLHandlerHints.toEntityResolver(hints));
    }

