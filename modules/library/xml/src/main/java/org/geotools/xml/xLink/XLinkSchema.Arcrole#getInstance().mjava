        // returns the default instance
        public static Attribute getInstance() {
            if (instance1 == null) {
                instance1 = new Arcrole();
            }

            return instance1;
        }

