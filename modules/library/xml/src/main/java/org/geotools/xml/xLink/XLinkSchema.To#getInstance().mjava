        /** Returns the singleton To instance */
        public static Attribute getInstance() {
            if (instance1 == null) {
                instance1 = new To();
            }

            return instance1;
        }

