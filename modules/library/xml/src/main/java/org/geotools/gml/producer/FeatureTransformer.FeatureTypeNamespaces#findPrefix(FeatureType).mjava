        public String findPrefix(FeatureType type) {
            String pre = lookup.get(type);

            if (pre == null) {
                pre = defaultPrefix;
            }

            return pre;
        }

