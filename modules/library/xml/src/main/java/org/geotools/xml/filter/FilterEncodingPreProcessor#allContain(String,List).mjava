    private boolean allContain(String fid, List fidSets) {
        for (Object fidSet : fidSets) {
            Set tmp = (Set) fidSet;

            if (!tmp.contains(fid)) {
                return false;
            }
        }

        return true;
    }

