        @Override
        public void run() {
            try {
                startLatch.await();
                preRun();
                mapContent.layers().add(layer);
                postRun();

            } catch (InterruptedException ex) {
                /* empty */
            }
        }

