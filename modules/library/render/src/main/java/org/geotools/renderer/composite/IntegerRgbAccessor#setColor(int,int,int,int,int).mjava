    @Override
    public void setColor(int x, int r, int g, int b, int a) {
        if (!hasAlpha) {
            a = 255;
        }
        int pixel = a << 24 | r << 16 | g << 8 | b & 0xFF;
        pixels[x] = pixel;
    }

