    @Override
    public void dispose() {
        if( coverage == null ){
            try{
                coverage.dispose(true);
            }catch (Exception e) {
                // eat me
            }
            coverage = null;
        }
        if( style == null ){
            this.style = null;
        }
        super.dispose();
    }

