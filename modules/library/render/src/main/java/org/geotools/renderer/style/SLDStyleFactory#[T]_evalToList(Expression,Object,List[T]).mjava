    private <T> List<T> evalToList(Expression exp, Object f, List<T> fallback) {
        if (exp == null) {
            return fallback;
        }
        List l = exp.evaluate(f, List.class);
        if (l != null) {
            return l;
        }
        return fallback;
    }

