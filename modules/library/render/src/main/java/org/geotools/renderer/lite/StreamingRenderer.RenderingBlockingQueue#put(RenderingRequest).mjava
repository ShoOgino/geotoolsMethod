        @Override
        public void put(RenderingRequest e) throws InterruptedException {
            if (!renderingStopRequested) {
                delegate.put(e);
                if (renderingStopRequested) {
                    this.clear();
                }
            }
        }

