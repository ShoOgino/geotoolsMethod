    /** Performs proper clean up on this {@link CoverageProcessingNode}. */
    private void cleanOutput() {
        if (executed) {
            executed = false;
            output.dispose(true);
            error = null;
        }
    }

