    /**
     * Build a {@Shape} WindBarb
     *
     * @return
     */
    Shape build() {
        if (knots < 0) {
            return buildAbsentModule();
        } else {
            return buildStandardBarb();
        }
    }

