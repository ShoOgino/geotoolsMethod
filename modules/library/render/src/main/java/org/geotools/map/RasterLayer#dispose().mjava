    @Override
    public void dispose() {
        // We assume that preDispose() has been called by the
        // by the sub-class

        if (source != null) {
            getUserData().remove("source");
            source = null;
        }
        super.dispose();
    }

