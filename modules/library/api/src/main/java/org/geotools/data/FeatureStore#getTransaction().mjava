    /**
     * Gets the {@code Transaction} that this {@code FeatureStore} is currently operating against.
     *
     * <pre><code>
     * Transaction t = featureStore.getTransaction();
     * try {
     *     featureStore.addFeatures( features );
     *     t.commit();
     * } catch( IOException erp ){
     *     // something went wrong;
     *    java.util.logging.Logger.getGlobal().log(java.util.logging.Level.INFO, "", ex);
     *     t.rollback();
     * }
     * </code></pre>
     *
     * @return Transaction in use, or {@linkplain Transaction#AUTO_COMMIT}
     */
    Transaction getTransaction();

