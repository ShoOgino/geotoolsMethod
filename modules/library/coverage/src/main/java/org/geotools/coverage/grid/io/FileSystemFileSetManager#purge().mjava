    @Override
    public void purge() {
        if (!fileSet.isEmpty()) {
            String[] files = fileSet.toArray(new String[fileSet.size()]);
            for (String filePath : files) {
                removeFile(filePath);
            }
        }
    }

