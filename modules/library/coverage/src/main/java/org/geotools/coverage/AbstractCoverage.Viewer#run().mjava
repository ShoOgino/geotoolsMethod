        /**
         * Display the window in the event queue. Required because 'pack()' is invoked before
         * 'setVisible(true)'.
         */
        @Override
        public void run() {
            frame.pack();
            frame.setVisible(true);
        }

