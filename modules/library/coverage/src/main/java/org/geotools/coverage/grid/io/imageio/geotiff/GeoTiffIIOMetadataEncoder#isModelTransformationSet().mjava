    private boolean isModelTransformationSet() {
        for (double v : modelTransformation) {
            if (v != 0.0) {
                return true;
            }
        }

        return false;
    }

