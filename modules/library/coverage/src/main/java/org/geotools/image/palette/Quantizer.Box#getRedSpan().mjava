        /**
         * The red span (higher red - lowest red)
         * 
         * @return
         */
        public int getRedSpan() {
            if (spanr == -1) {
                updateSpans();
            }
            return spanr;
        }

