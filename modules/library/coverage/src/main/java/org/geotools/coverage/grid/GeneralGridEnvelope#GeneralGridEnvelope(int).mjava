    /**
     * Constructs an initially empty grid envelope of the specified dimension. This is used by
     * {@link #getSubGridEnvelope} before a grid envelope goes public.
     */
    private GeneralGridEnvelope(final int dimension) {
        index = new int[dimension * 2];
    }

