    private Layer find( String name ){
        for( Layer layer : capabilities.getLayerList() ){
            if( name.equals( layer.getName() )){
                return layer;
            }
        }
        return null;
    }

