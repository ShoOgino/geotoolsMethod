        public StepList clone() {
            StepList copy = new StepList();
            for (Step step : this) {
                copy.add(step.clone());
            }
            return copy;
        }

