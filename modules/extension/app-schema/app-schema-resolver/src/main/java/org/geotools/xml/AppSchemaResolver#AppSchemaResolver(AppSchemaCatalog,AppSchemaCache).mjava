    /**
     * Constructor.
     * 
     * @param catalog
     * @param cache
     */
    public AppSchemaResolver(AppSchemaCatalog catalog, AppSchemaCache cache) {
        this(catalog, true, cache);
    }

