    /** Restore the log level. */
    @After
    public void after() {
        if (ADJUST_LOGLEVEL) {
            getRootLogHandler().setLevel(rootLogLevel);
            LOGGER.setLevel(logLevel);
        }
    }

