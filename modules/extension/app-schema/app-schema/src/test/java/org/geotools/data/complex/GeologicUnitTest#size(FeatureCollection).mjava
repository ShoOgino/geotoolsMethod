    private int size(FeatureCollection features) {
        int size = 0;
        FeatureIterator i = features.features();
        try {
            for (; i.hasNext(); i.next()) {
                size++;
            }
        } finally {
            i.close();
        }
        return size;
    }

