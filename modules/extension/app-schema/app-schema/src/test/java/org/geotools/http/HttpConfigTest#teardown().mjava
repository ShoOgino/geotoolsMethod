    @AfterClass
    public static void teardown() {
        try {
            HTTP_SERVER.stop();
        } finally {
            // remove the test schema cache
            FileUtils.deleteQuietly(APP_SCHEMA_CACHE_DIR);
        }
    }

