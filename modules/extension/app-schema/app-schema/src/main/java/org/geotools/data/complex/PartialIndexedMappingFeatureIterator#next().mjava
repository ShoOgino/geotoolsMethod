    @Override
    public Feature next() {
        if (hasNext()) {
            // sum 1 to counter
            sourceFeaturesCounter++;
            return sourceIterator.next();
        } else return null;
    }

