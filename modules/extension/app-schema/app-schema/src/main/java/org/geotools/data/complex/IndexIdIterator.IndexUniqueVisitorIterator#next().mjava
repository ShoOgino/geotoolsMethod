        @Override
        public String next() {
            if (hasNext()) {
                // take value, make placeholder null
                String value = nextValue;
                nextValue = null;
                return value;
            } else return null;
        }

