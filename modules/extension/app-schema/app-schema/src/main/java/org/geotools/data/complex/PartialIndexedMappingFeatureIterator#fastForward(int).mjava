    /** Iterate until startIndex */
    private boolean fastForward(int start) {
        while (sourceFeaturesCounter < start) {
            if (hasNext()) {
                next();
            } else {
                return false;
            }
        }
        return hasNext();
    }

