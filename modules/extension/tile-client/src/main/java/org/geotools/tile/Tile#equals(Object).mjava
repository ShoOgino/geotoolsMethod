    @Override
    public boolean equals(Object other) {
        if (this == other) {
            return true;
        }
        if (!(other instanceof Tile)) {
            return false;
        }

        return getUrl().equals(((Tile) other).getUrl());
    }

