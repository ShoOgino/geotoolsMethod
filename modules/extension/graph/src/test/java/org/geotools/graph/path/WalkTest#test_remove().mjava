    public void test_remove() {
        Node n = builder().buildNode();
        Walk walk = new Walk();

        walk.add(n);
        assertFalse(walk.isEmpty());

        walk.remove(n);
        assertTrue(walk.isEmpty());
    }

