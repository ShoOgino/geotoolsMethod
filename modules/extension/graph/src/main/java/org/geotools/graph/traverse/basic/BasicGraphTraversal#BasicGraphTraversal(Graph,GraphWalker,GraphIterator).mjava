    /**
     * Constructs a new graph traversal.
     *
     * @param graph The graph being traversed.
     * @param walker The walker being traversed over the components of the graph.
     * @param iterator The iterator specifying the order in which to visit components of the graph.
     */
    public BasicGraphTraversal(Graph graph, GraphWalker walker, GraphIterator iterator) {
        m_graph = graph;
        m_walker = walker;
        setIterator(iterator);
    }

