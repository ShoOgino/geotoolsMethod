    public boolean containsValue(Object value) {
        for (Object o : values()) {
            Collection c = (Collection) o;
            if (c.contains(value)) return (true);
        }
        return (false);
    }

