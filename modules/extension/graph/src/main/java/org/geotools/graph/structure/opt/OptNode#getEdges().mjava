    /** @see Node#getEdges() */
    @Override
    public List<Edge> getEdges() {
        ArrayList<Edge> edges = new ArrayList<>();

        for (int i = 0; i < m_edges.length; i++) {
            edges.add(m_edges[i]);
        }

        return (edges);
    }

