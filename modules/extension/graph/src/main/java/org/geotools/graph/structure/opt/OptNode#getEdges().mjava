    /** @see Node#getEdges() */
    @Override
    public List<Edge> getEdges() {
        ArrayList<Edge> edges = new ArrayList<>();

        for (Edge m_edge : m_edges) {
            edges.add(m_edge);
        }

        return (edges);
    }

