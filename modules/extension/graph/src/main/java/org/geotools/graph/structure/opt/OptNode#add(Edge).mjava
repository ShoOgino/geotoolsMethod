    /** @see Node#add(Edge) */
    @Override
    public void add(Edge e) {
        for (int i = 0; i < m_edges.length; i++) {
            if (m_edges[i] == null) {
                m_edges[i] = e;
                return;
            }
        }
    }

