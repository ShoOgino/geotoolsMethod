    /**
     * This method find index based on object value in parameter and call update method on that
     * index
     *
     * @param value - object which will be updated
     */
    public void update(Object value) {
        Integer index = ((Integer) m_obj2index.get(value));
        if (index != null) update(index.intValue());
    }

