    void write(File f, String xsd) throws IOException {
        f.deleteOnExit();
        f.createNewFile();
        try (FileWriter w = new FileWriter(f)) {
            w.write(xsd);
            w.flush();
        }
    }

