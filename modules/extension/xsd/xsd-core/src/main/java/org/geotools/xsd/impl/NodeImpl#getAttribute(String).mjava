    public Node getAttribute(String name) {
        if (name == null) {
            return null;
        }

        for (Node att : attributes) {
            if (name.equals(att.getComponent().getName())) {
                return att;
            }
        }

        return null;
    }

