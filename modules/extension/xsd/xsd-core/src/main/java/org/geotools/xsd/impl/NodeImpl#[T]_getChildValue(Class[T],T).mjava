    public <T> T getChildValue(Class<T> clazz, T defaultValue) {
        Object o = getChildValue(clazz);

        if (o == null) {
            o = defaultValue;
        }

        return clazz.cast(o);
    }

