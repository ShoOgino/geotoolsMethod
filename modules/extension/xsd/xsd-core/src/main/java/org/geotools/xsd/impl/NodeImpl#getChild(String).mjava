    public Node getChild(String name) {
        if (name == null) {
            return null;
        }

        for (Node child : children) {
            if (name.equals(child.getComponent().getName())) {
                return child;
            }
        }

        return null;
    }

