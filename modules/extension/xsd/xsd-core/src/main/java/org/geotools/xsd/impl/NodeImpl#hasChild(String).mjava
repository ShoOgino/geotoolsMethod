    @Override
    public boolean hasChild(String name) {
        if (name == null) {
            return false;
        }

        for (Node child : children) {
            if (name.equals(child.getComponent().getName())) {
                return true;
            }
        }

        return false;
    }

