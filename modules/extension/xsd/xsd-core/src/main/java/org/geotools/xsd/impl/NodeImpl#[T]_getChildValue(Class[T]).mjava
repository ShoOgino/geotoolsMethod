    @Override
    public <T> T getChildValue(Class<T> clazz) {
        Node node = getChild(clazz);

        if (node != null) {
            return clazz.cast(node.getValue());
        }

        return null;
    }

