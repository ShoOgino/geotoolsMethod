    public void setColors(Color[] colors) {
        this.colors = colors;

        int count = 0;

        for (Color color : colors)
            if (color != null) {
                count++;
            }

        this.numColors = count;
    }

