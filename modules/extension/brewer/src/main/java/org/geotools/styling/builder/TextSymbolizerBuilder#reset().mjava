    public TextSymbolizerBuilder<P> reset() {
        fill.reset(); // TODO: default fill for text?
        halo.unset(); // no default halo
        label.unset();
        geometry = null;
        labelPlacement = null;
        unset = false;
        return this;
    }

