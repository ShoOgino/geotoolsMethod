    /**
     * Build the expression and return to the parent builder.
     *
     * <p>Example use:<code>b.add().expr1().literal(1).end().expr2().literal(2).end().build();
     * </code>
     *
     * @see _build()
     */
    public P end() {
        build();
        return parent;
    }

