    public PointPlacementBuilder<P> reset(PointPlacement placement) {
        if( placement == null ){
            return reset();
        }
        rotation.reset( placement.getRotation() );
        unset = false; 
        return this;
    }

