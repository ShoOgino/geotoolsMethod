    public DefaultResourceInfo(FeatureSource fs) {
        this.fs = fs;
        words = new HashSet<>();
        {
            words.add("features");
            words.add(fs.getSchema().getName().toString());
        }
    }

